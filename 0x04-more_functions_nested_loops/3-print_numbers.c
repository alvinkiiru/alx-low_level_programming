#include "main.h"

/**
 * print_numbers - prints numbers between 0 through 9 and adds a new line.
 *
 * Return: Always 0 (success).
 */
void print_numbers(void)
{
 int c;

 for (c = 48; c < 58; c++)
 {
 _putchar(c);
 }
 _putchar('\n');
}

