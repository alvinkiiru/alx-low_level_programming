#include "main.h"

/**
 * main - used to test whether an integer is positive or negatve and prints
 * Return: 0 (success)
 */

int main(void)
{
	int i;

	i=98;
	positive_or_negative(i);

	return(0);
}
